// Moved to `@/common/utils/relative-time` — more than one feature needs it now
// (notifications and visit notes). Re-exported here so existing importers and
// the colocated test keep working.
export { formatRelativeTime } from "@/common/utils/relative-time";
